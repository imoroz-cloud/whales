name: Whale Alerts

# GitHub's own `schedule:` trigger was unreliable (long silent gaps, see
# repo history) -- an external cron-job.org job now calls workflow_dispatch
# every 5 minutes instead. Keeping both double-runs this for no benefit.
on:
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: whale-alerts
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Run whale check
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: node whale-check.mjs

      - name: Persist state
        run: |
          if ! git diff --quiet -- state.json; then
            git config user.name "whale-alerts-bot"
            git config user.email "actions@users.noreply.github.com"
            git add state.json
            git commit -m "chore: update whale-alert state [skip ci]"
            for i in 1 2 3 4 5; do
              if git push; then
                break
              fi
              echo "push rejected (probably raced newsjack-alerts' own push) -- rebasing and retrying ($i/5)"
              git fetch origin main
              git rebase origin/main
            done
          else
            echo "No state changes to commit"
          fi
